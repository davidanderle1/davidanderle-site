# Portable JSON Schema bootstrap checkpoint

This branch exists only to close the original R7 portable JSON Schema Draft 2020-12 requirement. BEARING is frozen. `main` and production deployment are out of scope.

The authoritative verification workflow may run only after the packed source contains a single typed Zod authority, deterministic committed Draft 2020-12 schemas, Ajv validation of checked-out records, and negative drift controls.
